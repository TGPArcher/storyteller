import { ToStoryUrlPipe } from './to-story-url.pipe';

describe('ToStoryUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ToStoryUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
